package io.github.sebkoo.hapsum.core.designsystem

import androidx.compose.material3.ColorScheme
import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.lightColorScheme

/** Material 3 baseline schemes — the single place `:app` and future features get color from. */
val HapsumLightColorScheme: ColorScheme = lightColorScheme()
val HapsumDarkColorScheme: ColorScheme = darkColorScheme()
