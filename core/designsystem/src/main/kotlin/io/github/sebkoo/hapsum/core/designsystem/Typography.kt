package io.github.sebkoo.hapsum.core.designsystem

import androidx.compose.material3.Typography

/** Material 3 baseline type scale — the single place `:app` and future features get type from. */
val HapsumTypography: Typography = Typography()
