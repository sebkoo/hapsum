{
  "includeCoAuthoredBy": false,
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "command -v jq >/dev/null || exit 0; f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.kt|*.kts) cd \"$CLAUDE_PROJECT_DIR\" && ./gradlew -q spotlessApply >/dev/null 2>&1 || true ;; esac"
          }
        ]
      }
    ]
  }
}
